apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kollie.fullname" . }}-test-connection"
  labels:
    {{- include "kollie.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kollie.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
